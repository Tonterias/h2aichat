# Dependencias de TEST/DESARROLLO (versiones fijadas — Fase 37.1).
# Incluye el runtime. Instala con:  pip install -r requirements-dev.txt
-r requirements.txt
pytest==9.0.3
playwright==1.61.0
pip-audit==2.10.0
